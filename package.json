{
  "name": "node-js-crud-app",
  "version": "1.0.0",
  "description": "Nodejs JWT Authentication and User Crud app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd frontend && npm install",
    "start": "node server.js",
    "client": "cd frontend && npm start",
    "dev": "concurrently \"npm start\" \"npm run client\"",
    "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "node.js",
    "jwt",
    "authentication",
    "express",
    "mysql"
  ],
  "author": "BilelFS",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.3"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  },
  "engines": {
    "node": "12.18.0"
  }
}
